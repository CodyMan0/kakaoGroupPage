import styled from 'styled-components';

const MessageForm = () => {
  return <Container>MessageForm</Container>;
};

export default MessageForm;

const Container = styled.div`
  flex: 2;
  padding: 40px;
  height: 100%;
  overflow-y: scroll;
`;
